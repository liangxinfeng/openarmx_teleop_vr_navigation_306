PAGE = r'''<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
  <meta name="theme-color" content="#06131f">
  <title>云蝶 酒店场景多任务编排中心</title>
  <style>
    :root{color-scheme:dark;--bg:#020a12;--panel:#071725;--panel2:#0a2131;--line:#1b536b;--cyan:#55ddf5;--blue:#6286ff;--mint:#62edb0;--amber:#ffd175;--red:#ff7188;--text:#effcff;--muted:#86adbd;--safe-top:env(safe-area-inset-top,0px);--safe-bottom:env(safe-area-inset-bottom,0px)}
    *{box-sizing:border-box;-webkit-tap-highlight-color:transparent}html,body{width:100%;height:100%;margin:0;overflow:hidden;background:var(--bg);color:var(--text);font-family:Inter,system-ui,"Microsoft YaHei",sans-serif;touch-action:manipulation}button,input{font:inherit}button{color:inherit}.app{height:100dvh;display:grid;grid-template-rows:auto auto minmax(0,1fr);padding:calc(12px + var(--safe-top)) 14px calc(12px + var(--safe-bottom));gap:10px;background:radial-gradient(circle at 10% -10%,#164c70 0,transparent 34%),radial-gradient(circle at 96% 110%,#0c4c48 0,transparent 34%),#020a12}.topbar{display:flex;align-items:center;justify-content:space-between;gap:14px;min-height:56px;padding:5px 5px 3px}.brand small{display:block;color:var(--cyan);font-size:10px;font-weight:900;letter-spacing:.22em}.brand h1{margin:4px 0 0;font-size:clamp(20px,2.6vw,31px);line-height:1.1;letter-spacing:.02em}.brand h1 span{color:#a8eefa}.tools{display:flex;align-items:center;gap:8px}.live,.icon-btn{height:44px;border:1px solid #285e73;border-radius:13px;background:#071a27;display:flex;align-items:center;gap:9px;padding:0 13px}.live{color:#9fc4d1;font-size:12px}.live.online{color:var(--mint)}.live.reconnecting{color:var(--amber)}.dot{width:8px;height:8px;border-radius:50%;background:currentColor;box-shadow:0 0 12px currentColor}.icon-btn{min-width:44px;justify-content:center;cursor:pointer}.icon-btn:active{transform:scale(.96);background:#10334a}.tabs{justify-self:center;display:grid;grid-template-columns:1fr 1fr;gap:5px;width:min(480px,100%);padding:5px;border:1px solid #21516a;border-radius:16px;background:#061522cc;box-shadow:0 12px 35px #0005}.tab{height:48px;border:0;border-radius:12px;background:transparent;color:#84adbe;font-weight:800;cursor:pointer;transition:.2s}.tab.active{color:#fff;background:linear-gradient(120deg,#126a86,#315499);box-shadow:0 7px 20px #1d8aa33b}.tab:active{transform:scale(.98)}.viewport{min-height:0;overflow:hidden;border:1px solid #1c5068;border-radius:22px;background:linear-gradient(145deg,#081c2bdd,#04111bdd);box-shadow:0 22px 70px #0008;touch-action:pan-y}.track{height:100%;width:200%;display:flex;transform:translateX(0);transition:transform .32s cubic-bezier(.22,.8,.25,1);will-change:transform}.track.page-1{transform:translateX(-50%)}.pane{width:50%;height:100%;min-width:0;padding:16px;overflow:hidden}.voice-layout{height:100%;display:grid;grid-template-columns:minmax(0,1.5fr) minmax(300px,.7fr);gap:14px}.panel{min-height:0;border:1px solid #1a465b;border-radius:17px;background:linear-gradient(155deg,#092031df,#05131fdf)}.mission{display:grid;grid-template-rows:auto auto minmax(0,1fr);padding:17px}.section-head{display:flex;align-items:center;justify-content:space-between;gap:12px}.eyebrow{color:var(--cyan);font-size:10px;font-weight:900;letter-spacing:.17em}.section-head h2{margin:4px 0 0;font-size:18px}.counter{min-width:65px;height:42px;display:grid;place-items:center;border:1px solid #31738c;border-radius:13px;color:var(--cyan);font-weight:900;background:#0b2c3e}.mission-status{display:grid;grid-template-columns:auto 1fr;align-items:center;gap:10px;margin:12px 0;padding:11px 13px;border:1px solid #1d5268;border-radius:13px;background:#092436}.state-pill{white-space:nowrap;color:var(--mint);font-size:12px;font-weight:900}.mission-message{min-width:0;color:#dff8ff;font-size:14px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.task-grid{min-height:0;overflow:auto;display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));align-content:start;gap:10px;padding:2px 3px 5px;scrollbar-width:thin;overscroll-behavior:contain}.task-card{position:relative;min-height:140px;padding:13px;border:1px solid #1b4b60;border-radius:15px;background:#071a27;overflow:hidden}.task-card:before{content:"";position:absolute;inset:0 auto 0 0;width:3px;background:#28556b}.task-card.current{border-color:#43d5ee;background:linear-gradient(145deg,#0d3449,#092134);box-shadow:0 0 24px #45ddf41d}.task-card.current:before{background:var(--cyan)}.task-card.done{opacity:.65}.task-card.done:before{background:var(--mint)}.task-card.failed{border-color:#894051}.task-top{display:flex;justify-content:space-between;align-items:center}.task-index{display:grid;place-items:center;width:32px;height:32px;border-radius:10px;background:#10384d;color:#9fd8e8;font-weight:900}.task-phase{font-size:11px;color:var(--muted)}.task-location{margin-top:12px;font-size:20px;font-weight:900}.task-action{margin-top:6px;color:#a8d1df;font-size:14px}.height{position:absolute;left:13px;right:13px;bottom:12px;display:flex;align-items:center;justify-content:space-between;color:#75cba7;font-size:11px}.height-bars{display:flex;align-items:end;gap:3px}.height-bars i{display:block;width:4px;height:6px;border-radius:3px;background:#174554}.height-bars i:nth-child(2){height:8px}.height-bars i:nth-child(3){height:10px}.height-bars i:nth-child(4){height:12px}.height-bars i:nth-child(5){height:14px}.height-bars i.on{background:var(--mint);box-shadow:0 0 7px #55e6a0}.empty{height:100%;min-height:180px;grid-column:1/-1;display:grid;place-items:center;text-align:center;color:#6e98a9}.empty strong{display:block;color:#b5d9e4;font-size:18px;margin-bottom:6px}.command{padding:17px;display:flex;flex-direction:column}.command .intro{margin:10px 0 14px;color:#85aebd;line-height:1.6;font-size:13px}.text-input{width:100%;height:52px;border:1px solid #275a70;border-radius:13px;background:#03101a;color:#fff;padding:0 14px;outline:none}.text-input:focus{border-color:var(--cyan);box-shadow:0 0 0 3px #4ce9ff17}.record{position:relative;flex:1;min-height:120px;margin-top:11px;border:1px solid #41c8e4;border-radius:17px;background:radial-gradient(circle at 50% 45%,#15758e,#113558 70%);color:#fff;font-size:18px;font-weight:900;overflow:hidden;cursor:pointer}.record:before{content:"";position:absolute;width:110px;height:110px;left:50%;top:50%;transform:translate(-50%,-50%);border:1px solid #7bf2ff66;border-radius:50%;box-shadow:0 0 45px #44e0fa29}.record:active{transform:scale(.985)}.record.recording{border-color:var(--red);background:radial-gradient(circle,#a93252,#5b1731);animation:pulse 1.2s infinite}.record:disabled,.dispatch:disabled{opacity:.5;cursor:wait}.record span{position:relative;z-index:1}.status{min-height:42px;display:flex;align-items:center;color:#82acbc;font-size:12px;line-height:1.4}.catalog-layout{height:100%;display:grid;grid-template-columns:minmax(330px,.9fr) minmax(390px,1.1fr);gap:14px}.library,.sequence{padding:16px;display:grid;grid-template-rows:auto minmax(0,1fr);gap:12px}.library-grid{min-height:0;overflow:auto;display:grid;grid-template-columns:repeat(auto-fit,minmax(155px,1fr));align-content:start;gap:9px;padding:2px;overscroll-behavior:contain}.library-card{position:relative;min-height:112px;padding:14px 44px 13px 13px;border:1px solid #1e5065;border-radius:15px;background:linear-gradient(145deg,#09263a,#071824);text-align:left;cursor:pointer;transition:.15s}.library-card:active{transform:scale(.97);border-color:var(--cyan)}.library-location{color:var(--cyan);font-size:12px;font-weight:900}.library-task{margin-top:8px;font-size:16px;font-weight:850;line-height:1.25}.add{position:absolute;right:10px;top:50%;transform:translateY(-50%);width:30px;height:30px;display:grid;place-items:center;border-radius:10px;background:#14465d;color:#86edff;font-size:20px}.sequence{grid-template-rows:auto minmax(0,1fr) auto}.queue-head-actions{display:flex;gap:7px}.small-btn{height:38px;border:1px solid #285b70;border-radius:11px;background:#0a2130;padding:0 12px;cursor:pointer}.small-btn:active{transform:scale(.96)}.queue{min-height:0;overflow:auto;display:flex;flex-direction:column;gap:8px;padding:2px;overscroll-behavior:contain}.queue-empty{height:100%;min-height:160px;display:grid;place-items:center;border:1px dashed #285568;border-radius:15px;text-align:center;color:#6f98a8;padding:25px}.queue-card{display:grid;grid-template-columns:48px 38px minmax(0,1fr) auto;align-items:center;gap:8px;min-height:68px;padding:8px;border:1px solid #1f5368;border-radius:14px;background:#082031;transition:border-color .12s,transform .12s}.queue-card.dragging{position:relative;z-index:8;border-color:var(--cyan);box-shadow:0 14px 35px #0009;transform:scale(1.015);opacity:.92}.drag-handle{height:48px;border:0;border-radius:11px;background:#10364a;color:#8ed4e8;font-size:21px;cursor:grab;touch-action:none}.drag-handle:active{cursor:grabbing}.queue-number{color:#6e9cac;font-weight:900;text-align:center}.queue-copy{min-width:0}.queue-copy strong{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.queue-copy small{display:block;margin-top:3px;color:#8db3c1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.queue-tools{display:grid;grid-template-columns:38px 38px 38px;gap:5px}.queue-tools button{height:38px;border:1px solid #26566a;border-radius:10px;background:#0b2939;cursor:pointer}.queue-tools button:active{transform:scale(.94)}.queue-tools .remove{color:#ff9bae}.dispatch-row{display:grid;grid-template-columns:1fr auto;gap:10px;align-items:center;padding-top:2px}.queue-tip{color:#719bad;font-size:12px}.dispatch{min-width:190px;height:52px;border:1px solid #56e0c0;border-radius:14px;background:linear-gradient(110deg,#12735e,#1d6680);font-weight:900;cursor:pointer;box-shadow:0 8px 25px #1b98773b}.dispatch:active{transform:scale(.98)}.toast{position:fixed;z-index:40;left:50%;bottom:calc(22px + var(--safe-bottom));transform:translate(-50%,30px);max-width:min(500px,88vw);padding:12px 17px;border:1px solid #39849c;border-radius:13px;background:#082638ee;color:#e9fbff;box-shadow:0 16px 50px #000a;opacity:0;pointer-events:none;transition:.22s}.toast.show{opacity:1;transform:translate(-50%,0)}.toast.error{border-color:#a04b60;color:#ffd4dc}@keyframes pulse{50%{box-shadow:0 0 38px #ff557044}}
    @media(max-width:900px){.app{padding-left:9px;padding-right:9px}.live span:last-child{display:none}.pane{padding:10px}.voice-layout,.catalog-layout{grid-template-columns:1fr;grid-template-rows:minmax(0,1.25fr) minmax(210px,.75fr)}.catalog-layout{grid-template-rows:minmax(250px,.9fr) minmax(300px,1.1fr)}.record{min-height:88px}.command .intro{display:none}.library-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.dispatch{min-width:160px}}
    @media(max-width:560px){.app{gap:7px;padding-top:calc(7px + var(--safe-top));padding-bottom:calc(7px + var(--safe-bottom))}.topbar{min-height:48px}.brand small{display:none}.brand h1{font-size:18px}.live{height:40px;padding:0 10px}.icon-btn{height:40px}.tabs{width:100%;height:52px}.tab{height:42px}.pane{padding:8px}.mission,.command,.library,.sequence{padding:12px}.voice-layout{grid-template-rows:minmax(0,1.4fr) minmax(170px,.6fr);gap:8px}.catalog-layout{grid-template-rows:minmax(215px,.8fr) minmax(300px,1.2fr);gap:8px}.task-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.task-card{min-height:125px}.task-location{font-size:17px}.library-card{min-height:91px}.queue-card{grid-template-columns:42px 28px minmax(0,1fr) auto}.queue-tools{grid-template-columns:32px 32px 32px}.queue-tools button{height:36px}.dispatch-row{grid-template-columns:1fr}.dispatch{width:100%}.queue-tip{display:none}}
    @media(orientation:landscape) and (max-height:620px){.app{padding-top:6px;padding-bottom:6px;gap:6px}.topbar{min-height:42px}.brand h1{font-size:18px}.tabs{position:absolute;z-index:12;top:8px;left:50%;transform:translateX(-50%);width:340px}.viewport{margin-top:0}.pane{padding:9px}.mission,.command,.library,.sequence{padding:12px}.record{min-height:90px}.task-card{min-height:124px}}
    /* Tablet-safe fixed viewport: the page itself never needs to scroll. */
    .app{height:100vh;height:100dvh}
    .library,.sequence{min-height:0;overflow:hidden}
    .library-grid,.queue{-webkit-overflow-scrolling:touch;touch-action:pan-y;overscroll-behavior:contain}
    .transcript{margin-top:8px;padding:9px 11px;border:1px solid #25566b;border-radius:12px;background:#061823;min-height:52px;max-height:74px;overflow:auto;color:#dff8ff;font-size:13px;line-height:1.45;overflow-wrap:anywhere}
    .transcript span{display:block;margin-bottom:3px;color:#64dff3;font-size:10px;font-weight:900;letter-spacing:.12em}
    @media(max-width:900px){.catalog-layout{grid-template-rows:minmax(140px,.45fr) minmax(0,1.55fr)}.library,.sequence{padding:10px}.library-card{min-height:78px;padding-top:10px;padding-bottom:9px}.queue-card{min-height:54px}.dispatch{height:46px}}
    @media(orientation:landscape) and (max-height:720px){.catalog-layout{grid-template-columns:minmax(0,.72fr) minmax(0,1.28fr);grid-template-rows:minmax(0,1fr)}.library,.sequence{padding:9px}.queue-card{min-height:50px}.drag-handle{height:38px}.queue-tools button{height:34px}.dispatch{height:44px}}
    /* Keep task text and the height indicator in separate layout rows. */
    .task-card{display:flex;flex-direction:column}
    .task-action{line-height:1.35;overflow-wrap:anywhere}
    .height{position:static;left:auto;right:auto;bottom:auto;margin-top:auto;padding-top:10px;flex:0 0 auto}
    /* Portrait tablets need larger touch targets; landscape remains unchanged. */
    @media(orientation:portrait) and (max-width:900px){.library-card{min-height:156px;padding:18px 48px 17px 16px}.library-task{font-size:17px;line-height:1.35}.add{width:36px;height:36px;font-size:23px}}
  </style>
</head>
<body>
<div class="app">
  <header class="topbar">
    <div class="brand"><small>YUNDIE · MISSION CONSOLE</small><h1>云蝶 酒店场景<span>多任务编排中心</span></h1></div>
    <div class="tools"><div id="live" class="live reconnecting"><i class="dot"></i><span>实时链路连接中</span></div><button id="fullscreen" class="icon-btn" aria-label="全屏">⛶</button></div>
  </header>
  <nav class="tabs" aria-label="任务编排方式"><button class="tab active" data-tab="0">语音任务</button><button class="tab" data-tab="1">卡片编排</button></nav>
  <main id="viewport" class="viewport">
    <div id="track" class="track">
      <section class="pane">
        <div class="voice-layout">
          <article class="panel mission">
            <div class="section-head"><div><div class="eyebrow">LIVE MISSION FLOW</div><h2>任务全览与实时进度</h2></div><div id="planCount" class="counter">0 / 0</div></div>
            <div class="mission-status"><span id="phase" class="state-pill">等待指令</span><span id="message" class="mission-message">说出连续任务，系统会立即生成完整任务链</span></div>
            <div id="planTasks" class="task-grid"><div class="empty"><div><strong>等待生成任务路径</strong><span>新任务会自动显示，无需刷新页面</span></div></div></div>
          </article>
          <aside class="panel command">
            <div class="section-head"><div><div class="eyebrow">VOICE / TEXT</div><h2>语音指令</h2></div></div>
            <p class="intro">点击大按钮开始录音，再次点击结束。也可以输入文字后按 Enter。</p>
            <input id="text" class="text-input" autocomplete="off" placeholder="输入任务后按 Enter">
            <button id="record" class="record"><span>开始录音并生成任务</span></button>
            <div id="status" class="status">本地语音识别已就绪</div>
            <div class="transcript"><span>本次语音识别</span><strong id="transcript">等待语音输入</strong></div>
          </aside>
        </div>
      </section>
      <section class="pane">
        <div class="catalog-layout">
          <article class="panel library">
            <div class="section-head"><div><div class="eyebrow">TASK LIBRARY</div><h2>航点任务卡片</h2></div><span id="catalogCount" class="counter">0 项</span></div>
            <div id="library" class="library-grid"></div>
          </article>
          <article class="panel sequence">
            <div class="section-head"><div><div class="eyebrow">TOUCH SEQUENCE</div><h2>任务执行顺序</h2></div><div class="queue-head-actions"><button id="undo" class="small-btn">撤销</button><button id="clear" class="small-btn">清空</button></div></div>
            <div id="queue" class="queue"></div>
            <div class="dispatch-row"><span class="queue-tip">触摸左侧手柄拖动排序，也可使用上下按钮</span><button id="dispatch" class="dispatch">分发 0 项任务</button></div>
          </article>
        </div>
      </section>
    </div>
  </main>
</div>
<div id="toast" class="toast"></div>
<script>
const BOOTSTRAP=__BOOTSTRAP_STATE__;
const UI_VERSION=String(BOOTSTRAP.ui_version||'');
const $=id=>document.getElementById(id);
const labels={idle:'等待指令',pending_confirmation:'等待确认',preparing:'任务准备中',navigating:'正在前往任务地点',navigation_failed:'导航未到达，等待VR处理',adjusting_height:'姿态调整中',awaiting_action:'等待任务完成',completed:'全部任务已完成',failed:'任务需要处理',cancelled:'任务已取消'};
const store={tab:0,state:null,catalog:[],catalogRevision:'',queue:[],history:[],busy:false,recording:false,stream:null};
let toastTimer=null,mediaStream=null,audioContext=null,audioSource=null,processor=null,silentGain=null,chunks=[];
function safeTasks(tasks){return Array.isArray(tasks)?tasks.filter(x=>x&&typeof x==='object').map(x=>({id:String(x.id||`${x.location||''}::${x.task||''}`),location:String(x.location||'未命名地点'),task:String(x.task||'未指定任务'),body_height_level:Math.max(1,Math.min(5,Number(x.body_height_level)||5))})):[]}
function signature(tasks){return JSON.stringify(safeTasks(tasks).map(x=>[x.location,x.task,x.body_height_level]))}
function toast(text,error=false){const el=$('toast');el.textContent=text;el.className=`toast show${error?' error':''}`;clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.className='toast',2600)}
function clearElement(element){while(element.firstChild)element.removeChild(element.firstChild)}
function setTab(index){store.tab=index===1?1:0;$('track').classList.toggle('page-1',store.tab===1);document.querySelectorAll('.tab').forEach((el,i)=>el.classList.toggle('active',i===store.tab))}
document.querySelectorAll('.tab').forEach(el=>el.onclick=()=>setTab(Number(el.dataset.tab)));
function heightBars(level){let html='<span class="height-bars">';for(let i=1;i<=5;i++)html+=`<i class="${i<=level?'on':''}"></i>`;return html+'</span>'}
function selectSnapshot(payload){const workflow=payload&&payload.task_dispatch||{},generated=payload&&payload.generated_plan||{};if(generated.tasks&&generated.tasks.length&&signature(generated.tasks)!==signature(workflow.tasks))return generated;return workflow}
function renderPlan(){const snapshot=selectSnapshot(store.state||{}),tasks=safeTasks(snapshot.tasks),state=String(snapshot.state||'idle'),index=Number(snapshot.current_index)||0;$('phase').textContent=labels[state]||state;$('phase').style.color=state==='completed'?'var(--mint)':state==='failed'?'var(--red)':'var(--mint)';$('message').textContent=snapshot.message|| (tasks.length?'任务已生成，等待执行端确认分发':'说出连续任务，系统会立即生成完整任务链');$('planCount').textContent=`${tasks.length?Math.min(tasks.length,index+1):0} / ${tasks.length}`;const box=$('planTasks');clearElement(box);if(!tasks.length){box.innerHTML='<div class="empty"><div><strong>等待生成任务路径</strong><span>新任务会自动显示，无需刷新页面</span></div></div>';return}tasks.forEach((item,i)=>{let phase=i<index?'done':i===index?'current':'';if(state==='completed')phase='done';if(state==='failed'&&i===index)phase='failed';const card=document.createElement('article');card.className=`task-card ${phase}`;card.innerHTML=`<div class="task-top"><span class="task-index">${phase==='done'?'✓':String(i+1).padStart(2,'0')}</span><span class="task-phase">${phase==='done'?'已完成':phase==='current'?'当前任务':'等待执行'}</span></div><div class="task-location"></div><div class="task-action"></div><div class="height"><span>身体高度 ${item.body_height_level}档</span>${heightBars(item.body_height_level)}</div>`;card.querySelector('.task-location').textContent=item.location;card.querySelector('.task-action').textContent=item.task;box.appendChild(card)})}
function loadQueue(){try{return safeTasks(JSON.parse(localStorage.getItem('yundie.taskQueue.v2')||'[]')).map(x=>({...x,uid:crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random()}`}))}catch(_){return[]}}
function saveQueue(){try{localStorage.setItem('yundie.taskQueue.v2',JSON.stringify(store.queue.map(({uid,...x})=>x)))}catch(_){}}
function reconcileQueue(){const allowed=new Map(store.catalog.map(x=>[`${x.location}::${x.task}`,x]));store.queue=store.queue.map(item=>{const current=allowed.get(`${item.location}::${item.task}`);return current?{...current,uid:item.uid}:null}).filter(Boolean);saveQueue()}
function renderCatalog(){const box=$('library');clearElement(box);$('catalogCount').textContent=`${store.catalog.length} 项`;store.catalog.forEach(item=>{const card=document.createElement('button');card.className='library-card';card.innerHTML='<div class="library-location"></div><div class="library-task"></div><span class="add">＋</span>';card.querySelector('.library-location').textContent=item.location;card.querySelector('.library-task').textContent=item.task;card.onclick=()=>addTask(item);box.appendChild(card)})}
function rememberQueue(){store.history.push(store.queue.map(x=>({...x})));if(store.history.length>20)store.history.shift()}
function addTask(item){rememberQueue();store.queue.push({...item,uid:crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random()}`});saveQueue();renderQueue();if(navigator.vibrate)navigator.vibrate(16);toast(`已加入：${item.location} · ${item.task}`)}
function syncQueueFromDom(){const byId=new Map(store.queue.map(x=>[x.uid,x]));store.queue=[...$('queue').querySelectorAll('.queue-card')].map(el=>byId.get(el.dataset.uid)).filter(Boolean);saveQueue();renderQueue()}
function bindDrag(handle){handle.addEventListener('pointerdown',event=>{if(event.button!==undefined&&event.button!==0)return;const card=handle.closest('.queue-card'),container=$('queue');rememberQueue();handle.setPointerCapture(event.pointerId);card.classList.add('dragging');const move=e=>{e.preventDefault();const target=document.elementFromPoint(e.clientX,e.clientY)?.closest('.queue-card');if(!target||target===card||target.parentElement!==container)return;const rect=target.getBoundingClientRect();container.insertBefore(card,e.clientY<rect.top+rect.height/2?target:target.nextSibling)};const end=()=>{card.classList.remove('dragging');handle.removeEventListener('pointermove',move);handle.removeEventListener('pointerup',end);handle.removeEventListener('pointercancel',end);syncQueueFromDom()};handle.addEventListener('pointermove',move);handle.addEventListener('pointerup',end);handle.addEventListener('pointercancel',end)})}
function renderQueue(){const box=$('queue');clearElement(box);if(!store.queue.length){box.innerHTML='<div class="queue-empty">点击左侧任务卡片加入序列<br>加入后可拖动调整顺序</div>'}store.queue.forEach((item,index)=>{const card=document.createElement('article');card.className='queue-card';card.dataset.uid=item.uid;card.innerHTML=`<button class="drag-handle" aria-label="拖动排序">≡</button><span class="queue-number">${String(index+1).padStart(2,'0')}</span><div class="queue-copy"><strong></strong><small></small></div><div class="queue-tools"><button class="up" aria-label="上移">↑</button><button class="down" aria-label="下移">↓</button><button class="remove" aria-label="删除">×</button></div>`;card.querySelector('strong').textContent=item.location;card.querySelector('small').textContent=item.task;card.querySelector('.up').onclick=()=>moveTask(index,-1);card.querySelector('.down').onclick=()=>moveTask(index,1);card.querySelector('.remove').onclick=()=>removeTask(index);bindDrag(card.querySelector('.drag-handle'));box.appendChild(card)});$('dispatch').textContent=`分发 ${store.queue.length} 项任务`;$('dispatch').disabled=store.busy||!store.queue.length}
function moveTask(index,delta){const next=index+delta;if(next<0||next>=store.queue.length)return;rememberQueue();[store.queue[index],store.queue[next]]=[store.queue[next],store.queue[index]];saveQueue();renderQueue()}
function removeTask(index){rememberQueue();store.queue.splice(index,1);saveQueue();renderQueue()}
$('clear').onclick=()=>{if(!store.queue.length)return;rememberQueue();store.queue=[];saveQueue();renderQueue();toast('任务序列已清空')};
$('undo').onclick=()=>{const previous=store.history.pop();if(!previous)return toast('没有可撤销的操作');store.queue=previous;saveQueue();renderQueue();toast('已撤销上一步')};
function applyState(payload){if(!payload||typeof payload!=='object')return;if(payload.ui_version&&payload.ui_version!==UI_VERSION){location.replace(`/?v=${encodeURIComponent(payload.ui_version)}&_=${Date.now()}`);return}store.state=payload;const catalog=payload.catalog||{};if(catalog.revision&&catalog.revision!==store.catalogRevision){const nextCatalog=safeTasks(catalog.tasks);store.catalog=nextCatalog;reconcileQueue();renderCatalog();renderQueue();store.catalogRevision=catalog.revision}renderPlan();const live=$('live');live.className=`live ${payload.online===false?'reconnecting':'online'}`;live.querySelector('span').textContent=payload.online===false?'任务已同步 · 执行端连接中':'任务状态实时同步'}
async function jsonRequest(url,options={}){const response=await fetch(url,{cache:'no-store',...options}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||data.message||'请求未完成');return data}
let fallbackBusy=false;
async function syncState(){if(fallbackBusy)return;fallbackBusy=true;try{applyState(await jsonRequest(`/api/state?_=${Date.now()}`))}catch(_){const live=$('live');live.className='live reconnecting';live.querySelector('span').textContent='实时链路重连中'}finally{fallbackBusy=false}}
function connectEvents(){if(store.stream)store.stream.close();const stream=new EventSource('/api/events');store.stream=stream;stream.addEventListener('state',event=>{try{applyState(JSON.parse(event.data))}catch(_){}});stream.onopen=()=>{const live=$('live');live.className='live online';live.querySelector('span').textContent='任务状态实时同步'};stream.onerror=()=>{const live=$('live');live.className='live reconnecting';live.querySelector('span').textContent='实时链路重连中'}}
function applySubmit(data,message){if(data.task_dispatch){store.state={...(store.state||{}),server_epoch:data.server_epoch,plan_revision:data.plan_revision,generated_plan:data.generated_plan||data.task_dispatch,task_dispatch:data.task_dispatch};renderPlan()}toast(data.submitted?message:'任务已生成，执行端正在连接')}
$('dispatch').onclick=async()=>{if(store.busy||!store.queue.length)return;store.busy=true;renderQueue();try{const data=await jsonRequest('/api/catalog-plan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({tasks:store.queue})});applySubmit(data,'卡片任务已分发');setTab(0);await syncState()}catch(error){toast(error.message,true)}finally{store.busy=false;renderQueue()}};
function encodeWav(parts,rate){const size=parts.reduce((sum,p)=>sum+p.length,0);if(size<rate*.18)throw new Error('录音时间太短');let samples=new Float32Array(size),offset=0;for(const part of parts){samples.set(part,offset);offset+=part.length}const outRate=16000,ratio=rate/outRate,out=new Float32Array(Math.floor(size/ratio));for(let i=0;i<out.length;i++){const a=Math.floor(i*ratio),b=Math.max(a+1,Math.min(size,Math.floor((i+1)*ratio)));let sum=0;for(let j=a;j<b;j++)sum+=samples[j];out[i]=sum/(b-a)}const buffer=new ArrayBuffer(44+out.length*2),view=new DataView(buffer),write=(at,text)=>[...text].forEach((c,i)=>view.setUint8(at+i,c.charCodeAt(0)));write(0,'RIFF');view.setUint32(4,36+out.length*2,true);write(8,'WAVE');write(12,'fmt ');view.setUint32(16,16,true);view.setUint16(20,1,true);view.setUint16(22,1,true);view.setUint32(24,outRate,true);view.setUint32(28,outRate*2,true);view.setUint16(32,2,true);view.setUint16(34,16,true);write(36,'data');view.setUint32(40,out.length*2,true);for(let i=0,o=44;i<out.length;i++,o+=2){const sample=Math.max(-1,Math.min(1,out[i]));view.setInt16(o,sample<0?sample*32768:sample*32767,true)}return new Blob([buffer],{type:'audio/wav'})}
async function startRecording(){mediaStream=mediaStream||await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true,channelCount:1}});audioContext=audioContext||new AudioContext();await audioContext.resume();audioSource=audioSource||audioContext.createMediaStreamSource(mediaStream);chunks=[];processor=audioContext.createScriptProcessor(2048,1,1);silentGain=silentGain||audioContext.createGain();silentGain.gain.value=0;processor.onaudioprocess=event=>{if(store.recording)chunks.push(new Float32Array(event.inputBuffer.getChannelData(0)))};audioSource.connect(processor);processor.connect(silentGain);silentGain.connect(audioContext.destination);store.recording=true;$('record').classList.add('recording');$('record').querySelector('span').textContent='结束录音并生成任务';$('status').textContent='正在录音，请完整说出任务';$('transcript').textContent='正在聆听…'}
function stopGraph(){store.recording=false;try{audioSource.disconnect(processor)}catch(_){}try{processor.disconnect()}catch(_){}processor=null;$('record').classList.remove('recording')}
async function stopAndSubmit(){const parts=chunks.slice(),rate=audioContext.sampleRate;chunks=[];stopGraph();store.busy=true;$('record').disabled=true;$('record').querySelector('span').textContent='正在识别与编排';$('status').textContent='正在生成任务链';try{const data=await jsonRequest('/api/audio',{method:'POST',headers:{'Content-Type':'audio/wav'},body:encodeWav(parts,rate)});$('transcript').textContent=String(data.transcript||'未识别到有效语句');if(data.tasks&&data.tasks.length){applySubmit(data,'语音任务已生成并分发');$('status').textContent='任务已生成，可继续录入新任务'}else{$('status').textContent='本次没有编排出有效任务';toast('本次没有编排出有效任务',true)}}catch(error){$('status').textContent='可以再次录音';toast(error.message,true)}finally{store.busy=false;$('record').disabled=false;$('record').querySelector('span').textContent='开始录音并生成任务'}}
async function submitText(){const text=$('text').value.trim();if(!text||store.busy)return;store.busy=true;$('record').disabled=true;$('status').textContent='正在生成任务链';try{const data=await jsonRequest('/api/plan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text})});if(data.tasks&&data.tasks.length){applySubmit(data,'文字任务已生成并分发');$('text').value='';$('status').textContent='任务已生成，可继续输入'}else{$('status').textContent='本次没有编排出有效任务';toast('本次没有编排出有效任务',true)}}catch(error){$('status').textContent='可以继续输入或录音';toast(error.message,true)}finally{store.busy=false;$('record').disabled=false}}
$('record').onclick=async()=>{if(store.busy)return;try{store.recording?await stopAndSubmit():await startRecording()}catch(error){stopGraph();toast(error.message||'麦克风不可用',true)}};$('text').addEventListener('keydown',event=>{if(event.key==='Enter')submitText()});
$('fullscreen').onclick=async()=>{try{document.fullscreenElement?await document.exitFullscreen():await document.documentElement.requestFullscreen()}catch(_){toast('当前浏览器未开放全屏权限',true)}};
let swipe=null;$('viewport').addEventListener('touchstart',event=>{if(event.touches.length!==1||event.target.closest('button,input,.queue-card'))return;swipe={x:event.touches[0].clientX,y:event.touches[0].clientY}},{passive:true});$('viewport').addEventListener('touchend',event=>{if(!swipe||!event.changedTouches.length)return;const dx=event.changedTouches[0].clientX-swipe.x,dy=event.changedTouches[0].clientY-swipe.y;swipe=null;if(Math.abs(dx)>70&&Math.abs(dx)>Math.abs(dy)*1.35)setTab(dx<0?1:0)},{passive:true});
store.queue=loadQueue();applyState(BOOTSTRAP);renderQueue();connectEvents();syncState();setInterval(syncState,2000);document.addEventListener('visibilitychange',()=>{if(!document.hidden)syncState()});
</script>
</body>
</html>'''
